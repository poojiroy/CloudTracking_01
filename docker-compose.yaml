version: '3.8'

services:
  cloudtrack-app:
    build: .
    image: cloudtrack-app:latest
    container_name: cloudtrack-app
    working_dir: /app
    env_file:
      - .env
    ports:
      - "8000:8000"
    command: uvicorn main:app --host 0.0.0.0 --port 8000
